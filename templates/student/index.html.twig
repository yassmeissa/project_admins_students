<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord Étudiant</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #667eea;
            --secondary-color: #764ba2;
            --success-color: #27ae60;
            --light-bg: #f8f9fa;
            --dark-text: #2c3e50;
            --border-color: #e0e0e0;
        }

        body {
            {% if theme == 'dark' %}
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #ecf0f1;
            {% else %}
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: var(--dark-text);
            {% endif %}
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .welcome-card {
            background: white;
            border-radius: 20px;
            padding: 50px 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        .welcome-card h1 {
            font-size: 36px;
            font-weight: 700;
            color: var(--dark-text);
            margin-bottom: 10px;
        }

        .welcome-card p {
            font-size: 18px;
            color: #7f8c8d;
            margin-bottom: 40px;
        }

        .welcome-icon {
            font-size: 64px;
            color: var(--primary-color);
            margin-bottom: 30px;
        }

        .action-buttons {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 30px;
        }

        .btn-action {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            border: none;
            color: white;
            padding: 15px 25px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .btn-action:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .btn-action i {
            font-size: 20px;
        }

        .btn-theme {
            background: var(--primary-color);
            border: none;
            color: white;
            padding: 12px 20px;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-theme:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
        }

        {% if theme == 'dark' %}
        .welcome-card {
            background: #34495e;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }

        .welcome-card h1,
        .welcome-card .greeting {
            color: #ecf0f1;
        }

        .welcome-card p {
            color: #bdc3c7;
        }

        .btn-action {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .btn-action:hover {
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.6);
        }
        {% endif %}

        @media (max-width: 768px) {
            .welcome-card {
                padding: 30px 20px;
            }

            .welcome-card h1 {
                font-size: 28px;
            }

            .action-buttons {
                grid-template-columns: 1fr;
            }

            .btn-action {
                padding: 12px 20px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="welcome-card">
        <div class="welcome-icon">
            <i class="fas fa-graduation-cap"></i>
        </div>
        
        <h1 class="greeting">Bienvenue, {{ student.username }}! 👋</h1>
        <p>Accédez à vos cours et participez aux forums</p>

        <div class="action-buttons">
            <a href="{{ path('student_courses', {'idS': student_id}) }}" class="btn-action">
                <i class="fas fa-book"></i>
                Mes Cours
            </a>
            <a href="{{ path('post_forum_index', {'idS': student_id}) }}" class="btn-action">
                <i class="fas fa-comments"></i>
                Forums
            </a>
        </div>

        <form action="{{ path('app_student_change_theme', {'idS': student_id}) }}" method="post" style="margin: 0;">
            <input type="hidden" name="theme" value="{{ theme == 'light' ? 'dark' : 'light' }}">
            <button type="submit" class="btn-theme">
                <i class="fas fa-{{ theme == 'light' ? 'moon' : 'sun' }}"></i>
                {{ theme == 'light' ? 'Mode Sombre' : 'Mode Clair' }}
            </button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
